{
  "_comment": "Copy this file to serve.config.json and fill in your OpenRouter key. serve.config.json is gitignored. Prefer the OPENROUTER_API_KEY environment variable if you can — it never touches disk. Restart the server after changing the key. The tutor uses Qwen 3.5 Flash with reasoning disabled for low latency.",
  "openrouterApiKey": "sk-or-...",
  "models": {
    "tutor": "qwen/qwen3.5-flash-02-23"
  }
}
